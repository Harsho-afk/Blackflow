server:
    port: 8080
    routes:
        /auth:
            interval: 5s
            algorithm: least_connection
            backends:
                - http://localhost:8081
                - http://localhost:8082
        /api:
            interval: 10s
            algorithm: least_connection
            backends:
                - http://localhost:8091
